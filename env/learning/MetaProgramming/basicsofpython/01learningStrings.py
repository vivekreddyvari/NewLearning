# strings:
a_string = 'Suhruth Seyon & Karunya Reddyvari'

# string: Center
print(f"The a_string center = {a_string.center(1)}")

# string: count
print(f"the count of chars `u` in the string = {a_string.count('u')}")

# string: ljust
print(f"The a_string left justified = {a_string.ljust(9, '*')}")

# string: lower
print(f"The a_string lower case = {a_string.lower()}")

# string: rjust
print(f"The a_string right justified = {a_string.rjust(10)}")

# string: find
print(f"The a_string find `r` = {a_string.find('r')}")

# string: split
print(f"The a_string split = {a_string.split(' ')}")


"""The major difference between lists and strings is that lists can be modified while strings cannot
This is referred as mutability """